# .github/CODEOWNERS
# Who must review & approve changes before they can merge to main.
# Format:  <path pattern>   <@github-username(s)>
# Each owner must be a collaborator with Write access who accepted the invite.
# With TWO owners, either one can approve the other's PR (no self-approval needed).

# foodiego-frontend is frontend-only, so the whole repo is owned by both approvers:
*   @Kalimuthu-certainti   @prithinga-certainti
